---
label: Product
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Название
  description: 'Введите название продукта'
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: 'Описание '
  description: 'Введите описание продукта'
- name: category
  type: select
  default: []
  config:
    required: false
    options: []
    source:
      type: pages
      section: caterories
      file:
      path:
  label: 'Категория '
  description: 'Выберите категорию продукта'
- name: image
  type: file
  config:
    maxSize: '20'
  default: "/uploads/logo3.png"
  label: 'Основная картинка '
  description: Выберите основную картинку
- name: price
  type: number
  default: 0
  required: true
  config:
    min:
    max:
    step:
    required: true
  label: Цена
  description: Введите актуальную цену
- name: old_price
  type: number
  default: '0'
  required: true
  config:
    min:
    max:
    step:
  label: 'Старая цена (если есть)'
  description: Введите старую цену до скидки
- name: image_gallery
  type: image_gallery
  config: {}
  label: Галерея
  description: Выберите дополнительные картинки для слайдера
- name: size
  type: select
  default: ''
  config:
    required: false
    options:
    - XS
    - S
    - M
    - L
    - XL
    source:
      type: simple
      section:
      file:
      path:
  label: Размер
  description: Выберите размер, если есть
pages:
- products/t-shirt.md
- products/Kids/kids-dress.md
